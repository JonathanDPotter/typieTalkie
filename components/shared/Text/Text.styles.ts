import { StyleSheet } from "react-native";
import { colors, textSize } from "../../../sharedStyles";

const styles = StyleSheet.create({
  text: { fontSize: textSize, color: colors.orange },
});

export default styles;
