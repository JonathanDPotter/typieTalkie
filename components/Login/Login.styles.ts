import { StyleSheet } from "react-native";
import { colors } from "../../sharedStyles";

const styles = StyleSheet.create({
  login: { flex: 1, backgroundColor: colors.darkGreen },
});

export default styles;
